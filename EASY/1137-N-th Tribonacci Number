class Solution {
    public int tribonacci(int n) {
        int first = 0;
        int second = 1;
        int third = 1;

        while (n-- > 0) {
            int next = first + second + third;
            first = second;
            second = third;
            third = next;
        }

        return first;
    }
}
